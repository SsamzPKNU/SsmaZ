# 팀 워크플로우 및 30일 개발 계획
> **작성일**: 2026-01-16  
> **프로젝트**: 학원 관리 웹 서비스 (project3)  
> **마감일**: D-30

---

## 1. 팀 구성 및 현황

### 팀 구성
| 역할 | 인원 | 경력 | 비고 |
|------|------|------|------|
| 리드 개발자 | 1명 | 경험자 | 백엔드, DB, AI 담당 |
| 프론트엔드 | 3명 | 비전공자 (6개월차) | 바이브코딩 활용 |

### 현재 완료된 작업
- 기초 백엔드 구조 (FastAPI)
- 기초 DB 설계 (MySQL)
- 기초 프론트엔드 구조 (React + Vite)
- GitHub 레포지토리 및 브랜치 세팅

### 팀원 기술 수준
- React 기초 구조 이해 (useState, useEffect, props)
- ChatGPT 등 AI 도구를 활용한 바이브코딩 선호
- 디버깅 및 에러 해결 능력 제한적

---

## 2. 역할 분담

### 리드 개발자 (본인)
- 백엔드 API 개발 (FastAPI)
- 데이터베이스 설계 및 관리 (MySQL)
- AI 모델 연동 (문자 리뷰 자동 생성)
- 프론트엔드 템플릿 코드 제공
- 코드 리뷰 및 기술 지원

### 프론트엔드 팀원
| 팀원 | 담당 영역 | 난이도 |
|------|-----------|--------|
| A | 대시보드, 출결 체크 UI | 중 |
| B | 학생 관리, 반 관리 UI | 중 |
| C | 공통 컴포넌트, QA, 문서 정리 | 하 |

---

## 3. 30일 로드맵

### 1주차 (D-30 ~ D-23): 기반 작업

#### 리드 개발자
| 일차 | 작업 내용 |
|------|-----------|
| 1-2일 | 핵심 API 명세서 작성 및 공유 |
| 3-4일 | 학생 CRUD API 완성 |
| 5-6일 | 출결 체크 API 완성 |
| 7일 | 프론트 템플릿 페이지 1개 완성 (학생 목록) |

#### 프론트엔드 팀원
| 일차 | 작업 내용 |
|------|-----------|
| 1-3일 | 개발 환경 세팅 확인, Git 사용법 복습 |
| 4-7일 | 템플릿 코드 분석 및 이해 |

### 2주차 (D-22 ~ D-16): 핵심 기능 개발

#### 리드 개발자
- 반 목록 API
- 대시보드 통계 API
- 결제/수납 API
- 코드 리뷰 (주 2회)

#### 프론트엔드 팀원
| 팀원 | 작업 내용 |
|------|-----------|
| A | 대시보드 페이지 개발 |
| B | 학생 목록 페이지 개발 |
| C | 공통 컴포넌트 (버튼, 카드, 모달) |

### 3주차 (D-15 ~ D-9): 기능 완성

#### 리드 개발자
- AI 모델 연동 (문자 리뷰)
- 알림 시스템 구현
- 프론트 통합 지원

#### 프론트엔드 팀원
| 팀원 | 작업 내용 |
|------|-----------|
| A | 출결 체크 UI 완성 |
| B | 학생 등록/수정 폼 완성 |
| C | 반 목록 페이지, 에러 화면 |

### 4주차 (D-8 ~ D-1): 통합 및 QA

| 일차 | 전체 팀 작업 |
|------|--------------|
| D-8 ~ D-6 | 전체 기능 연결 테스트 |
| D-5 ~ D-3 | 버그 수정 및 UI 다듬기 |
| D-2 ~ D-1 | 최종 점검, 발표 준비 (해당 시) |

---

## 4. 바이브코딩 팀을 위한 가이드

### 4.1 페이지 템플릿

팀원들은 아래 템플릿을 복사하여 새 페이지를 만듭니다.

```jsx
// 📁 src/pages/[페이지명].jsx
// 이 파일을 복사해서 다른 페이지 만들기

import { useState, useEffect } from 'react';
import api from '../services/api';

export default function TemplatePage() {
  // ✅ 상태 관리: 데이터를 저장하는 변수
  const [data, setData] = useState([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState(null);

  // ✅ 페이지 로드 시 API 호출
  useEffect(() => {
    // TODO: 여기 API 주소만 바꾸면 됨
    api.get('/api주소')
      .then(res => setData(res.data))
      .catch(err => setError('데이터를 불러오지 못했습니다'))
      .finally(() => setLoading(false));
  }, []);

  // ✅ 로딩 중일 때
  if (loading) {
    return (
      <div className="flex justify-center items-center h-screen">
        <p className="text-gray-500">로딩중...</p>
      </div>
    );
  }

  // ✅ 에러 발생 시
  if (error) {
    return (
      <div className="flex justify-center items-center h-screen">
        <p className="text-red-500">{error}</p>
      </div>
    );
  }

  // ✅ 정상 화면
  return (
    <div className="p-4">
      {/* 페이지 제목 */}
      <h1 className="text-2xl font-bold mb-4">페이지 제목</h1>
      
      {/* 데이터 목록 */}
      <div className="space-y-2">
        {data.map(item => (
          <div 
            key={item.id} 
            className="border rounded-lg p-4 hover:bg-gray-50"
          >
            {/* TODO: 여기 표시할 내용 수정 */}
            <p className="font-medium">{item.name}</p>
          </div>
        ))}
      </div>
    </div>
  );
}
```

### 4.2 작업 지시서 형식

태스크 배정 시 아래 형식을 따릅니다.

```markdown
## 📋 작업: [작업명]

### 기본 정보
- 복사할 파일: `src/pages/TemplatePage.jsx`
- 새 파일명: `src/pages/[새파일명].jsx`
- 담당자: [이름]
- 예상 소요: [시간]

### API 정보
- 주소: `GET /api/[엔드포인트]`
- 응답 예시:
```json
{
  "id": 1,
  "name": "홍길동",
  "status": "재원"
}
```

### 표시할 필드
- name: 이름
- status: 상태

### 완료 기준
- [ ] 목록이 화면에 표시됨
- [ ] 로딩 상태가 보임
- [ ] 에러 시 메시지가 표시됨

### 참고 화면
[피그마/스크린샷 링크]
```

### 4.3 PR(Pull Request) 템플릿

```markdown
## 🎯 작업 내용
- 

## 📸 스크린샷
- 

## ✅ 체크리스트
- [ ] 로컬에서 화면 확인함
- [ ] 콘솔에 에러 없음
- [ ] 다른 페이지에 영향 없음

## 🤔 질문/논의 사항
- 
```

### 4.4 Git 브랜치 규칙

```
main                    # 배포용 (건드리지 않기)
└── deploy/홍길동       # 개인 개발 브랜치
└── deploy/김철수       # 개인 개발 브랜치
└── deploy/이영희       # 개인 개발 브랜치
```

**작업 흐름**
1. 본인 브랜치에서 작업
2. 커밋 & 푸시
3. GitHub에서 PR 생성
4. 리드 개발자 코드 리뷰
5. main에 머지

---

## 5. API 명세서 (1주차 내 확정)

### 인증 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | `/auth/signup` | 회원가입 |
| POST | `/auth/login` | 로그인 |
| POST | `/auth/logout` | 로그아웃 |

### 학생 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/students` | 학생 목록 조회 |
| GET | `/students/{id}` | 학생 상세 조회 |
| POST | `/students` | 학생 등록 |
| PUT | `/students/{id}` | 학생 정보 수정 |
| DELETE | `/students/{id}` | 학생 삭제 |

### 반(Class) API
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/classes` | 반 목록 조회 |
| GET | `/classes/{id}/students` | 반별 학생 목록 |
| POST | `/classes` | 반 생성 |

### 출결 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/attendance/today` | 오늘 출결 현황 |
| POST | `/attendance` | 출결 체크 |
| GET | `/attendance/student/{id}` | 학생별 출결 이력 |

### 대시보드 API
| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/dashboard/summary` | 대시보드 통계 |

---

## 6. 주간 미팅 체크리스트

### 매주 월요일 (킥오프)
- [ ] 이번 주 목표 공유
- [ ] 각자 담당 태스크 확인
- [ ] 블로커 이슈 공유

### 매주 목요일 (중간점검)
- [ ] 진행 상황 공유
- [ ] 코드 리뷰 진행
- [ ] 다음 스텝 조정

---

## 7. 리스크 관리

| 리스크 | 대응 방안 |
|--------|-----------|
| API 병목으로 프론트 작업 지연 | Mock 데이터로 선작업, API 명세 먼저 확정 |
| 바이브코딩 결과물 품질 저하 | 템플릿 제공, 코드 리뷰 강화 |
| 팀원 간 코드 스타일 불일치 | 템플릿 복사 방식으로 통일 |
| 통합 시 충돌 | 주 2회 코드 리뷰로 조기 발견 |
| 일정 지연 | 우선순위 기능만 MVP로 완성 |

---

## 8. MVP (최소 기능 제품) 우선순위

30일 내 반드시 완성해야 할 기능:

### 필수 (Must Have)
1. ✅ 로그인/로그아웃
2. 🔲 대시보드 (출결 현황)
3. 🔲 학생 목록 조회
4. 🔲 출결 체크 기능

### 권장 (Should Have)
5. 🔲 학생 등록/수정
6. 🔲 반 관리
7. 🔲 결제 현황 조회

### 선택 (Nice to Have)
8. 🔲 문자 알림 발송
9. 🔲 AI 리뷰 자동 생성
10. 🔲 통계 리포트

---

## 9. 연락 및 소통

### 소통 채널
- 일반 소통: [슬랙/카카오톡 채널명]
- 코드 리뷰: GitHub PR
- 긴급 이슈: [연락처]

### 질문 가이드
코드 관련 질문 시 아래 정보 포함:
1. 어떤 파일에서 작업 중인지
2. 무엇을 하려고 했는지
3. 어떤 에러가 발생했는지 (스크린샷)
4. 이미 시도해본 것

---

> **문서 업데이트**: 주요 변경사항 발생 시 이 문서를 업데이트합니다.